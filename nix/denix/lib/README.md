# nix/denix/lib

Reserved for shared helper functions that are specific to this repository's Denix modules.

The directory is intentionally kept in Git to make the extension point explicit even when no local helpers are required.
